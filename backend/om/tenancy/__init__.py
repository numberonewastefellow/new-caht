"""Tenant-context package.

INTEGRATOR NOTE: WS-M owns this package (canonical facade + provisioning/service).
This ``__init__`` is a placeholder so the ``rewrite/ws-d`` worktree imports
standalone; on merge, keep WS-M's package contents.
"""
