# WS-A — Purge (license paywall + billing) — status log

- **2026-07-19 14:22 AST** — Started. Created worktree `.claude/worktrees/ws-a` on branch `rewrite/ws-a` off `rename_onyx_to_om`. Read CONTRACTS.md + WS-A-purge.md. Oriented with graphify (license=community 165, billing=community 134). Mapped file layout: license cluster (`db/license.py`, `utils/license.py`, `server/license/`, `middleware/license_enforcement.py`, `configs/license_enforcement_config.py`, `keys/license_public_key.pem`) + billing cluster (`server/billing/`, `tenants/{billing,billing_api,proxy,product_gating}.py`) confirmed present. Identified main.py wiring (imports L63/L71/L73-75, routers L497/L501, middleware L688-695, EE log L715) and auth_check.py public-endpoint specs (L78-87). Dispatched 3 parallel Explore agents to map full backend-license / backend-billing / frontend dependency graphs before editing.
- **2026-07-19 14:27 AST** — All 3 dependency maps complete. Key findings:
  - `billing/api.py`, `tenants/proxy.py`, `tenants/billing_api.py` are DELETED wholesale (their internal license usage is moot). Real surgical work is in ~13 KEPT files.
  - **KEPT files needing edits:** `main.py` (routers+middleware+EE log), `auth_check.py` (proxy/stripe specs), `tenants/api.py` (billing/proxy sub-routers), `settings/api.py` (rewrite `apply_license_status_to_settings`, drop dead `check_ee_features_enabled`), `settings/models.py` (`ee_features_enabled`), `usage_limits.py` (import break on `fetch_billing_information`), `manage/users.py` (register_tenant_users + invalidate_license_cache), `auth/users.py` (`enforce_seat_limit`), `scim/api.py` (`_check_seat_availability`), `onyxbot/slack/listener.py` (`get_gated_tenants`/`is_tenant_gated`/license metadata), `onyxbot/slack/handlers/handle_message.py` (`check_seat_availability`), `onyxbot/discord/cache.py` (`get_gated_tenants`), `db/models.py` (`License` model L4543-4559).
  - **product_gating neutralization:** `get_gated_tenants()`->`set()`, `is_tenant_gated()`->`False` at the 3 kept call sites.
  - **No dedicated billing DB tables** — only `license` table to drop (confirms plan). Stripe SDK used ONLY in deleted `tenants/billing.py`. HubSpot in `provisioning.py` is signup-marketing (KEPT, not billing). control-plane/data-plane token helpers in `tenants/access.py` are SHARED (KEPT).
  - **Frontend:** self-contained deletes = `app/admin/billing/`, `lib/billing/`, `hooks/useBillingInformation.ts`+`useLicense.ts`, `GatedContentWrapper.tsx`+`errorPages/AccessRestrictedPage.tsx`, dead `sections/sidebar/AdminSidebar.tsx`. Surgical: `adminNavItems.ts`, `ClientLayout.tsx`, `app/layout.tsx`, `settings/interfaces.ts`, `settings/lib.ts`, `AnnouncementBanner.tsx`.
  - **Out of scope (left untouched):** Craft "message limit" usage paywall (WS-F usage territory, touches no billing/license endpoints); tenant data-isolation core (WS-M); `tenant_usage`/`tenant_usage_limits.py` (WS-F).
- **2026-07-19 14:52 AST** — **Phase 1 (backend) COMPLETE + reviewed.** 30 files deleted (license+billing modules + dedicated tests + keys/). Wiring cleaned: `main.py` (billing/license router imports+registrations, license-enforcement middleware `else` branch, "Running Enterprise Edition" log), `auth_check.py` (removed 8 stripe/proxy public-endpoint specs), `tenants/api.py` (billing+proxy sub-routers). Settings: dropped `apply_license_status_to_settings`/`check_ee_features_enabled` + `ee_features_enabled` field. Neutralized seat/gating callers: `enforce_seat_limit`→no-op, scim `_check_seat_availability`→None, slack `_check_tenant_gated`→False (+deleted orphan `_extract_channel_from_request`), slack handle_message seat block removed, discord cache `get_gated_tenants` gating removed, `usage_limits.is_tenant_on_trial`→False (billing import removed), manage/users `register_tenant_users`+`invalidate_license_cache` removed. Removed `License` ORM model + dead config (`STRIPE_*`, `GATED_TENANTS_KEY`, `LICENSE_ENFORCEMENT_ENABLED`). Tests fixed: deleted `test_slack_gating.py`, integration `test_seat_limit.py`; removed `TestSeatLimitEnforcement` class + `TestGatedTenantHandling` class; rewrote bulk-invite test 2; scim `test_user_endpoints.py` unchanged (patches kept no-op). **Review gate PASS:** all 18 modified .py files `py_compile` clean; ZERO active imports of deleted modules in `backend/om`; no orphaned imports (MULTI_TENANT/Session/cast/Any still used); no `import stripe`; no `License` model refs; no stale settings-fn refs. **Regen flagged (integrator, needs full image):** `migration_safety/snapshots/baseline.json` (module inventory) + `route_rename/snapshots/before/*.json` + `_openapi_schema.json` — recapture after deletion; do NOT hand-edit.
- **2026-07-19 15:01 AST** — **Phase 2 (frontend) COMPLETE + reviewed.** Deleted 7 self-contained surfaces: `app/admin/billing/` (page + PlansView/CheckoutView/BillingDetailsView/LicenseActivationCard + css), `lib/billing/` (index/interfaces/svc/svc.test), `hooks/useBillingInformation.ts`, `hooks/useLicense.ts`, `components/GatedContentWrapper.tsx`, `components/errorPages/AccessRestrictedPage.tsx`, dead `sections/sidebar/AdminSidebar.tsx`. Surgical edits: `ClientLayout.tsx` (removed billing hooks + `hasSubscription` + PAYMENT_REMINDER banner + Button/ApplicationStatus imports), `adminNavItems.ts` (removed Plan&Billing nav item + `hasSubscription` param + route label + path color + SvgWallet import), `app/layout.tsx` (removed GatedContentWrapper gating → children always render; removed productGating + imports), `settings/interfaces.ts` (removed `ee_features_enabled`), `header/AnnouncementBanner.tsx` (removed two_day_trial_ending billing banner + unused Route import). **DECISION:** kept `ApplicationStatus` enum + `application_status` field (always ACTIVE now) on BOTH frontend + backend for Settings-contract alignment — only the gating BEHAVIOR removed. **Review gate:** comprehensive grep across `web/src` = ZERO imports of deleted modules, ZERO refs to removed symbols (useLicense/useBillingInformation/hasActiveSubscription/hasSubscription/ee_features_enabled/GatedContentWrapper/SvgWallet/@stripe/loadStripe); all orphaned imports removed. `tsc --noEmit` NOT runnable in fresh worktree (no node_modules; junction fails type-resolution → 17k uniform env errors, none naming removed symbols) — **frontend `next build`/tsc is the integrator's gate** (full env + Docker rebuild). **Follow-up (documented, not done — lockfile risk):** `@stripe/stripe-js` + `stripe` npm deps in `web/package.json` now unused, removable with a `package-lock.json` regen.
- **2026-07-19 15:10 AST** — **Phase 3 COMPLETE + reviewed.** Added `backend/alembic/versions/0004_drop_license.py` (revision `0004_drop_license`, down_revision `0003_agent_rename`): `upgrade()` drops `license` table (+ its PK/index/owned seq); `downgrade()` recreates it from the baseline DDL. Revision chain verified linear/single-head (0001→0002→0003→0004). Did NOT run alembic (per project rule — backend applies on restart). **Boot/import review gate (static, no DB/full-env here):** all 19 changed .py files (18 modified + migration) `py_compile` clean; direct import test — `settings.models`, `usage_limits`, `scim.api`, `discord.cache` import fully clean; `auth_check`/`settings.api`/`tenants.api`/`manage.users`/`auth.users` fail ONLY on missing 3rd-party pkgs (`sendgrid`/`puremagic`/`braintrust`/`posthog` — partial system Python, unrelated to WS-A), never on any billing/license/undefined-name error. Removed routes (`/license*`, `/admin/billing*`, `/proxy/*`, `/tenants/stripe-publishable-key`) are unregistered (main.py + tenants/api.py) and their auth_check public-specs removed → will 404; `check_ee_router_auth` has no billing/license routes to validate. **NOTE:** installed `sendgrid`+`puremagic` into the system Python during the import check (harmless; both are real backend deps). Full multi-tenant boot is the integrator's Docker gate.
**CHANGESET:** 45 deleted, 23 modified, 1 added (migration). **Coordination held for integrator:** main.py wiring snippet (WS-M owns the tenant-middleware block), auth_check specs, tenants/api.py sub-router removal — all in `rewrite-plans/notes/WS-A-README.md`.


[REVIEW] 2026-07-19 15:43 AST — Rigorous self-review complete. **5 gaps found + fixed:** (1) **CRITICAL BUG** `onyxbot/slack/listener.py acquire_tenants` — removing the `gated_tenants` definition left a second loop (tenant re-validation) still referencing it → runtime NameError every Slack acquisition cycle; removed the dead gated-disconnect block. Caught by `mypy --strict name-defined` (grep had missed it). (2) Dead billing code `auth/email_utils.py send_subscription_cancellation_email` (0 callers) — removed. (3) 11 dead billing DTOs in `server/tenants/models.py` (BillingInformation/CreateCheckoutSessionRequest/ProductGating*/Stripe*/Subscription*/Checkout*, 0 external refs) — removed + now-unused datetime/Literal/ApplicationStatus imports; all remaining consumers import only kept tenant DTOs (verified). (4) Stale docstring `features/build/api/subscription_check.is_user_subscribed` (described deleted Stripe/license paths) — updated (behavior already correct: authed users → full access). (5) Stale doc `onyxbot/discord/DISCORD_MULTITENANT_README.md` "Enterprise Gating Support" showed removed `get_gated_tenants` code — removed. **Verification:** all 22 changed .py `py_compile` clean; `mypy` (project config, sqlalchemy plugin) on every changed backend file = clean except 3 PRE-EXISTING `email_utils.py` errors (confirmed identical on unchanged main tree); `test_cache_manager.py` **17 passed**; other 2 edited tests need full backend deps (integrator/CI gate) — statically traced + mypy/py_compile clean. Frontend: import-usage consistency verified per edited file (no dangling locals/imports); `tsc` remains integrator gate. Migration chain single linear head `0004`. **Env note:** installed sendgrid/puremagic/discord.py/posthog/braintrust into system Python during import/test checks (harmless; real backend deps).

[REVIEW-FIX] 2026-07-19 16:04 AST — Addressed the deferred cleanup follow-ups (dead deps + CI-blocking snapshot). **(1) Python stripe SDK dep REMOVED** from source-of-truth root `pyproject.toml` (backend extra) + `backend/requirements/default.txt` (Docker installs this via `uv pip install -r`) + its 2 stale `#%   stripe` annotations; `hubspot-api-client` kept. `uv.lock` left for tooling regen — `uv lock` fails in this worktree on a PRE-EXISTING `backend/pyproject.toml [tool.uv.sources]` workspace-member error (`om` not a member), and Docker reads default.txt not uv.lock. **(2) npm @stripe/stripe-js + stripe REMOVED** from `web/package.json`; regenerated `web/package-lock.json` via `npm install --package-lock-only` (diff is stripe-only, no unrelated bumps) — `npm ci` build stays in sync. **(3) migration_safety golden snapshot FIXED** (CI-blocking unit test): removed exactly the 14 deleted-module entries from `baseline.json.modules`, re-serialized in the test capture format; verified celery_tasks/beat_tasks/fetch_targets UNCHANGED by WS-A (0 billing/license entries each) → deterministic == recapture. **(4) route_rename before/ snapshots LEFT INTACT** — live-only (non-CI) rename harness with a coherent 480-endpoint baseline; partial hand-edit would corrupt coherence; recapture live if re-run. **Verify:** pyproject valid TOML, package.json valid JSON, baseline.json valid + diff=14 deletions/0 insertions; no Python source touched this pass (review mypy/py_compile state unchanged); test_cache_manager 17 passed (sanity). Env: also installed discord.py/posthog/braintrust into system Python for test runs.

[REVIEW] 2026-07-19 16:41 AST — Third rigorous pass (deeper semantic sweep beyond literal keywords). **Gaps found + fixed (4 classes):** (1) **Dead billing notification query** — `settings/api.get_settings_notifications` still queried `NotificationType.TRIAL_ENDS_TWO_DAYS` ("product gating notification") though nothing emits it post-billing-removal; removed the query (function now only surfaces reindex notifs). Verified no test asserts trial-notif behavior. (2) **Stale deployment/CI/build config** referencing removed vars — cleaned STRIPE_*/LICENSE_ENFORCEMENT_ENABLED from: 2 docker-compose files, `web/Dockerfile` (NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY ARG/ENV x2 stages), `.github/workflows/deployment.yml` (Stripe secret passthrough x2), 6 CI workflow files (LICENSE_ENFORCEMENT_ENABLED env blocks/inline/.env-heredocs), `tests/integration/Dockerfile`, and `tools/ods/cmd/compose.go` (removed the `setEnvValue("LICENSE_ENFORCEMENT_ENABLED",...)` call AND the now-orphaned `setEnvValue` func — verified all its imports used elsewhere). All 8 edited YAML re-validated (UTF-8). (3) **Dead conftest env-sets** — removed `LICENSE_ENFORCEMENT_ENABLED` env-sets in `daily/conftest.py` (+ orphaned `import os`/comment) and `migration_trigger/conftest.py`; fixed stale `migration_safety/README.md` run-command. (4) **Stale inline comment** in `subscription_check.py:39` ("check Stripe billing via control plane") — updated to match no-billing behavior. **Verification:** mypy on ALL changed backend files = clean except 3 PRE-EXISTING `email_utils.py` errors (identical on main tree); py_compile all 24 changed .py clean; `settings/api.py` mypy exit 0; `test_cache_manager.py` 17 passed; all YAML/TOML/JSON valid. **Left intentionally (out of scope, documented):** `scripts/tenant_cleanup/*` ops scripts (parse external control-plane CSV/DB `stripe_customer_id` columns — not billing feature code); route_rename live-only snapshot baseline; `application_status` field + `ApplicationStatus` enum (kept backend+frontend for Settings-contract alignment); trial invite cap in `manage/users` (WS-F usage territory, inert). Could not `go build` compose.go (no Go toolchain) — removal verified safe by import analysis. **Changeset now: 45 deleted, 46 modified, 1 new.**
