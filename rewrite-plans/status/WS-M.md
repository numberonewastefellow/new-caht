# WS-M — Multi-tenant data isolation — status log

Phase plan:
- P0 Wave0: publish Contract 3 (tenant import paths + public-schema table list)
- P1 Isolation core: tenancy/context.py + middleware + per-tenant migration runner + mapping table
- P2 Provisioning (billing-free): strip control-plane/Stripe/HubSpot/pool; on-demand create
- P3 Tenant-admin UI + routes (superuser-gated) + menu entry + structured logs
- P4 Delete old EE remnants + tenant-isolation smoke verify

- [P0] Started: read CONTRACTS.md + WS-M in full, graphify-oriented, mapped existing tenant files — now
- [P0] Worktree was on stale pre-rename baseline; fast-forwarded to rename_onyx_to_om (backend/om). Contract 3 FINALIZED + published to CONTRACTS.md. Public table = user_tenant_mapping. — now
- [P1] Started: building clean-room backend/om/tenancy/ package (context facade, middleware, migrations, models, repository, service). — now
- [2026-07-19 14:23] [P1] Resumed: reviewed existing tenancy files + verified facade imports against sql_engine/contextvars/env.py primitives. Research done (schema_translate_map>search_path; pure-ASGI middleware for correct contextvar propagation). Building middleware.py + Alembic mapping revision next.
- [2026-07-19 14:41] [P1] DONE: middleware.py (pure-ASGI tenant tracking, resolution API-key/PAT->session-redis->anon(gated)->tenant-cookie->default, 400 on bad schema); Alembic revision wsm_user_tenant_mapping.py (public, idempotent). Review PASS: sessions bind via schema_translate_map per-request contextvar (reset in finally), bg jobs use explicit get_tenant_session; resolution order verified; verified extract_tenant is parse-only + anon JWT tenant key='tenant_id'.
- [2026-07-19 14:41] [P2] DONE: provisioning.py (billing-free on-demand: create schema->migrate->setup_onyx->assign mapping; advisory-lock serialized; rollback on failure; NO stripe/hubspot/control-plane/pool) + service.py (TenantService: list/create/delete/deactivate/assign+move). Redirected 5 auth login call sites to om.tenancy.provisioning. Added repo.resolve_or_activate so invited users route correctly (no dup provision). Review PASS: no external calls; on-demand create path verified.
- [2026-07-19 14:41] [P3] Started: tenant-admin routes (om/server/tenancy/api.py, superuser-gated) + main.py wiring + web admin UI + Tenants menu.
- [2026-07-19 15:02] [P3] DONE: routes om/server/tenancy/api.py (GET/POST /admin/tenants, POST /{id}/users, POST /{id}/deactivate, DELETE /{id}); main.py wiring (tenant_admin_router + add_tenant_tracking_middleware replacing add_api_server_tenant_id_middleware). Web UI web/src/app/admin/tenants/** (page + create/assign modals + lib/types, VertualAI design, stat cards use --virtualai-accent). Tenants menu entry in Governance (admin-only). Web tsc --noEmit PASS (exit 0). Review PASS+FIX: caught check_router_auth boot-crash risk (custom dep not in recognized top-level list) -> routes now Depends(current_admin_user) + _enforce_platform_admin allow-list in-handler; structured events on all create/update/delete/assign.
- [2026-07-19 15:20] [P4] DONE + WS-M COMPLETE. Deleted old EE middleware om/server/middleware/tenant_tracking.py (fully replaced, 0 importers). Wrote backend/om/tenancy/README.md (isolation model, Contract 3 usage, resolution flow, config, structured events, research cites, EE-remnant deletion manifest). Verification: backend/tests/unit/om/tenancy/test_tenant_isolation.py 17 passed / 6 skipped(middleware needs Docker deps->importorskip). Grep-verified NO stripe/hubspot/control-plane calls in new om/tenancy/** + om/server/tenancy/**. Web tsc PASS. Coordinated EE drops (available_tenant pool + celery pre-provision + tenant_anonymous_user_path + old billing provisioning fns) documented in README manifest for integrator (cross-WS importers: celery/user-mgmt/WS-A billing; fresh DB->no migration).
- [2026-07-19 self-review] SELF-REVIEW + FIX. Found incomplete auth decoupling: P2 migrated the 5 get_or_provision_tenant sites but 3 login-routing sites still imported EE get_tenant_id_for_email (users.py authenticate + get_by_email + oauth callback via om.server.tenants.user_mapping) — the old EE tenant package stayed load-bearing for login (Phase-4 drop would have broken auth) and the new repo reads lacked the non-MULTI_TENANT default-schema guard. Fix: added om.tenancy.provisioning.get_login_tenant_id(email)->str|None (self-hosted->default schema, MT->resolve_or_activate, never provisions, None on unknown); repointed all 3 sites; users.py now has ZERO om.server.tenants refs. Fixed config.py docstring (removed reference to non-existent om.tenancy.settings; tenancy config is env-only by design, no billing/control-plane). Gated Tenants nav on enableCloud (NEXT_PUBLIC_CLOUD_ENABLED) so it only shows in multi-tenant. Added 3 login-routing tests. Verify: pytest tests/unit/om/tenancy/test_tenant_isolation.py = 26 passed; py_compile clean; grep om.server.tenants in users.py = none; grep stripe/hubspot/control_plane in om/tenancy = comments-only. Web tsc NOT run in worktree (no local TS install); nav change is a trivial in-scope conditional. Deferrals unchanged (integrator owns Alembic linearize + EE-remnant drops).
