# WS-H — Analytics / query-history / reporting / app-settings — status log

- **2026-07-19 11:10 AST** — Started. Created worktree `.claude/worktrees/ws-h` on branch `rewrite/ws-h` off `rename_onyx_to_om`. Read CONTRACTS.md (depends on Contract 3 tenant) + WS-H-analytics.md. graphify graph does NOT cover `backend/om` (indexes phoenix/docs) — attempted per rule, then explored source directly. Mapped EE-origin behavior surface (read for WHAT-to-build per CONTRACTS provenance clause; writing own HOW):
  - `server/analytics/api.py` + `db/analytics.py` — admin daily aggregates (queries/likes/dislikes, active users, onyxbot, per-agent messages/unique-users, per-assistant stats). Aggregate over `ChatMessage`/`ChatMessageFeedback`/`ChatSession`.
  - `server/query_history/api.py` — admin chat-session list (paginated, feedback filter, time range) + detail snapshot + async CSV export via Celery + file store.
  - `server/reporting/usage_export_api.py` + `usage_export_generation.py` — generate usage report (zip of chat_messages.csv + users.csv) via Celery, list, download. Old table `usage_report(s)` via `db/usage_export.py`.
  - `server/enterprise_settings/*` — KV-backed `EnterpriseSettings` (branding + chat customization + custom-analytics-script + logo); public GET drives frontend gating; SCIM token mgmt piggy-backed here; OAuth refresh-token endpoint.
- Research (recharts/dashboards): ResponsiveContainer in sized parent + `debounce`; memoize data/config, stable dataKey; color via CSS vars (`var(--virtualai-accent)`) for theme/dark auto-adapt; aggregate server-side to keep charts light. Cited in README.
- Dispatched 2 parallel mapping agents (backend infra: tenancy/models/auth/logging/main.py/alembic; frontend: recharts/admin pages/nav/design-system/fetch/gating).
- **Mapping results** — key facts: package is `om.*`; FastAPI DB dep = tenant-scoped `get_session` (om.db.engine.sql_engine), tenant id via `shared_configs.contextvars.get_current_tenant_id` (no `om.tenancy.context` on this branch yet — integrator swaps to the WS-M facade later). `ChatMessage.processing_duration_seconds` (Float) = latency source; `chat_feedback` keyed on `is_positive` bool|None. No JSON logging helper (built one). Alembic head `0003_agent_rename`, raw-SQL `op.execute` style. Frontend recharts via `@/components/ui/areaChart` wrapper (colors are literal strings — I introduce theme-aware CSS-var colors); menu = `web/src/sections/sidebar/AdminSidebar.tsx` `collections()`; settings shape = `EnterpriseSettings` in `web/src/app/admin/settings/interfaces.ts`; downloads via `Content-Disposition` + anchor.
- **Scope corrections (surfaced, not blindly executed):** `server/oauth/api.py` is *connector* OAuth (Slack/Confluence/GDrive) — deleting it breaks connectors; and SCIM token mgmt is entangled inside `enterprise_settings/api.py` (WS-G territory). Both will be PRESERVED and flagged for the integrator, not deleted.
- **2026-07-19 11:35 AST — Phase 1 COMPLETE + reviewed.** Added 3 per-tenant tables via own model block in `db/models.py` (banner `# ==== WS-H: analytics / reporting / app-settings models ====`): `analytics_rollup` (window_start/metric/value, unique+indexed), `usage_report` (report_name unique, file_id, requestor FK SET NULL, period, created_at indexed), `app_settings` (typed singleton backing web `enterpriseSettings` + `feature_flags` JSONB + custom_analytics_script). Alembic `0004_ws_h_analytics.py` (down_revision=None placeholder per CONTRACTS; raw schema-relative SQL) creates the 3 tables + 5 performance btree indexes on chat_message/chat_feedback/chat_session hot columns (previously only GIN fts indexes existed). Aggregation: `server/analytics/service.py::AnalyticsService` (tenant-scoped daily usage: queries/active-users/likes/dislikes/avg-latency; summary KPIs; top-agents bar; rollup upsert/read) — never opens its own session, operates on injected tenant-bound session. Structured JSON logging helper `server/analytics/structured_logging.py` (`log_structured_event` + `timed_event` CM, Standard-9 field contract, never raises). Settings config table backed by `server/app_settings/{models,repository,service}.py` (get-or-create singleton, load/save with `settings_updated` structured event). **Review gate PASS:** all 8 files `py_compile` clean; queries tenant-scoped (injected session, no schema kwarg); new tables + hot-path indexes present; settings schema mirrors frontend `EnterpriseSettings`.
- **2026-07-19 12:05 AST — Phase 2 COMPLETE + reviewed.** APIs (all admin-gated via `current_admin_user`, tenant-scoped via `get_session`):
  - **Analytics** (`server/analytics/api.py`, prefix `/analytics`): `GET /admin/usage` (daily series), `/admin/summary` (KPIs), `/admin/top-agents` (bar), `GET/POST /admin/rollups[/refresh]`.
  - **Query history** (`server/query_history/{models,service,api}.py`, prefix `/admin/query-history`): `GET /sessions` (paginated + feedback filter + time range), `GET /sessions/{id}` (detail), `GET /export` (synchronous streaming CSV of Q&A pairs — replaces the EE Celery async export; no file-store/Celery dependency).
  - **Reporting** (`server/reporting/{models,service,api}.py` NEW files, prefix `/admin/reports`): `POST /usage` (synchronous generate → daily-usage CSV to file store + `usage_report` row), `GET /usage` (list), `GET /usage/{id}/download` (stream CSV). Structured `report.generated` / `report_exported` events.
  - **App settings** (`server/app_settings/{models,repository,service,logo,api}.py` NEW module, prefixes `/enterprise-settings` + `/admin/enterprise-settings` — SAME paths as EE so web boot/gating is unaffected): public `GET ""` (drives gating) + `custom-analytics-script` + `logo`/`logotype`; admin `PUT ""` + `custom-analytics-script` + `logo`. `settings_updated` structured event on writes.
  - **Coupling flagged for Phase 4:** old query-history/reporting Celery tasks + `db/query_history.py` + cleanup bits + `scripts/chat_feedback_dump.py` + tests import old symbols my rewrite removed — these obsolete consumers get deleted together in Phase 4 (full `backend/om` import-consistency check there). New reporting files are net-new so old `usage_export_*` stay importable until then.
  - **Review gate PASS:** all 12 files `py_compile` clean; AST unused-import scan clean; **bug fixed** — settings `PUT` now does a partial update via `model_dump(exclude_unset=True)` so a branding-only save can't wipe `feature_flags`; dead `_to_columns` removed. CSV: query-history streams `text/csv` w/ `Content-Disposition`; reports stream stored CSV. Permission-gating verified per endpoint; public settings GETs need public-spec entries (in auth_check snippet).
- **2026-07-19 12:45 AST — Phase 3 COMPLETE + reviewed.** Frontend admin screens (VertualAI design system): `web/src/app/admin/performance/analytics/` (`page.tsx` KPI tiles + line/bar charts + date range; `charts.tsx` reusable theme-aware recharts wrappers; `lib.ts` types + SWR hooks), `performance/query-log/page.tsx` (paginated table + feedback filter + detail Modal + CSV export), `performance/reports/page.tsx` (generate + list + download), `admin/app-settings/page.tsx` (branding + chat customization + feature toggles form). Menu: 4 entries added to `AdminSidebar.tsx` `collections()` (Analytics/Query Logs/Reports in Performance; App Settings in Settings), inside the existing `!isCurator` admin block. **Review gate PASS:** charts use theme-aware CSS-var colors (`var(--virtualai-accent)`, `var(--theme-primary-*)`) — NO hardcoded accent colors; `ResponsiveContainer` + `debounce` in sized parents, `useMemo` data, animation off, stable dataKeys (recharts perf guidance); tables in `overflow-x-auto`. Verified every component/prop/import against source (AdminPageTitle/CardSection/Text/Button/Switch/InputTypeIn/InputTextArea/Table/PageSelector/Modal/Badge/AdminDateRangeSelector/errorHandlingFetcher/buildApiPath). **Fixed:** `SvgDownload`→`SvgDownloadCloud` (SvgDownload not exported), filter-button active state (`main`/`secondary`), latency-tile TS narrowing. tsc not runnable in fresh worktree (no node_modules) → frontend type-check is the integrator's gate.
- **2026-07-19 13:05 AST — Phase 4 (delete-old + verify) — DECISION + verify.** After mapping the consumer graph, deleting the coupled EE code mid-worktree is UNSAFE: `enterprise_settings/store.py` is used by `email_utils`/`onyx_runtime`/`seeding` (branding); old reporting/query-history helpers are wired into shared Celery **beat_schedule.py + primary.py + cleanup task**; and `main.py`/`auth_check.py`/Alembic are integrator-owned. So WS-H lands as **clean-room new files ALONGSIDE the EE originals** (the pattern already used for reporting/app_settings), keeping the worktree import-consistent, and the physical deletion is delegated to the integrator (safe on the fresh DB, avoids cross-WS collisions). Reverted my earlier in-place overwrites of `analytics/api.py` + `query_history/api.py` + `query_history/models.py` (they broke Celery/script/test consumers) and moved that code to `analytics/admin_api.py`, `query_history/admin_api.py` + `query_history/schemas.py`. **Verify:** whole WS-H set + restored originals + coupled db files ALL `py_compile` clean → tree import-consistent. Only 2 shared files touched: `db/models.py` (append under WS-H banner) + `AdminSidebar.tsx` (menu). `main.py`/`auth_check.py` NOT touched (snippets delivered in README §8). **Exhaustive integrator deletion checklist** written in README §9 (groups A–E, atomic per coupling) + **scope corrections**: `server/oauth/*` is connector OAuth (NOT deleted — would break connectors) and SCIM token mgmt in `enterprise_settings/api.py` is WS-G's (preserved) — both flagged (README §9-E).
- **README:** `backend/om/server/analytics/README.md` — architecture, tables, metrics/aggregation, endpoints, frontend/charts, log events, research citations, main.py + auth_check snippets, integrator deletion checklist, extension guide.
- **Not done (by design):** no commit/push; no `alembic upgrade` run; main.py/auth_check.py wiring + coupled EE deletion left to integrator (checklist provided). Frontend `tsc`/`next build` + backend full-image import test are the integrator's gates (no node_modules / full deps in the isolated worktree).

- **[REVIEW] 2026-07-19 (self-review) — gaps found & fixed:** (1) **Bug** — query-history CSV export lazily queried the DB on the *request-scoped* session, but a `StreamingResponse` body is consumed after the handler returns → refactored to `stream_query_history_csv(tenant_id, …)` which opens its own tenant-bound session via `get_session_with_tenant` for the stream's lifetime (still tenant-isolated). (2) **Standard 9** — logo upload wasn't structured-logged → added a `settings_updated` event (`field=logo|logotype`). (3) **mypy** (1.11.2, available in-worktree) caught `db_session=` passed to `FileStore.save_file`/`read_file` — invalid on the abstract base (typed return of `get_default_file_store()`); the EE original didn't pass it → removed, file store resolves the tenant from the contextvar (synchronous in-handler, still tenant-scoped). (4) **Standard 10** — parametrized bare generics (`list`→`list[Any]`/`list[DailyUsagePoint]`, `list[list]`→`list[list[Any]]`, `Sequence`→`Sequence[Any]`). **Verify:** all WS-H backend files `py_compile` clean; mypy clean on all changed files (2 real errors → fixed); AST unused-import scan clean; auth audit — every admin endpoint gated, 4 settings/logo GETs intentionally public (branding pre-login, in auth_check spec). **Confirmed no gaps:** clean-room (no old-EE imports in new code); multi-tenant-safe (all queries + streaming export on tenant-bound sessions; logs carry tenant_id); no hardcoded accent colors (grep clean); menu entries admin-gated. **Standing deviations (unchanged, justified):** tenant imports use `sql_engine.get_session` + `shared_configs.contextvars.get_current_tenant_id` (the Contract-3 facade `om.tenancy.context` is ABSENT on this branch — confirmed; integrator swaps when WS-M lands); physical deletion of coupled EE code delegated to the integrator (main.py/auth_check integrator-owned per CONTRACTS; `enterprise_settings/store.py` used by email/runtime/seeding; old helpers wired into shared Celery beat/primary) — full checklist in README §9. **Not run:** frontend `tsc` (no node_modules in worktree) + full-image backend import test = integrator gates; no unit tests added (no test-coverage standard; DB-backed integration tests are the integrator's gate).
